import ClassesIndexContent from './ClassesIndexContent'

export const metadata = {
  title: 'Classes — SattvaFlow',
  description: 'All offerings at SattvaFlow — yoga, movement, healing, and gatherings.',
}

export default function ClassesIndexPage() {
  return <ClassesIndexContent />
}
